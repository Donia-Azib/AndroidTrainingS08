<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.androidtrainings08">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.CAMERA"/>
<!--    + code fel java (besoin)    -->

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidTrainingS08">
        <activity android:name=".ActivityMenu" android:label=""/>
        <activity
            android:name=".RegisterActivity"
            android:theme="@style/Theme.AndroidTrainingS08.NoactionBar" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.AndroidTrainingS08.NoactionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>